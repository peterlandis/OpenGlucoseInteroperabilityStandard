# OGIS examples

JSON files in this directory **must** validate against [glucose.reading.v0_1.json](../schemas/jsonschema/glucose.reading.v0_1.json).

Run validation locally:

```bash
npm install
npm run validate:examples
```
